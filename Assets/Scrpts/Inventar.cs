using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Inventar : MonoBehaviour
{

    public int tamaño = 0;

    // Start is called before the first frame update
    void Start()
    {
        PlayerSize.text = tamaño;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
